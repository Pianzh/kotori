/target
# 设计稿 crate(ui-shell)自己的构建产物:上面那条是锚定到根目录的,管不到它
/ui-shell/target/
**/*.rs.bk
Cargo.lock
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Local agent working doc - deliberately untracked
AGENTS.md

# Project goals - same deal: kept on disk, deliberately untracked
/GOALS.md

# Local config / real data - NEVER commit
config.toml
*.local.toml
kotori.sock

# Credentials / secrets (B2 keys, kopia passwords) - NEVER commit
*.key
*.secret
.env
.env.*
credentials*.json
*kopia-password*

# 沙箱里构建用的独立 cargo 缓存（~/.cargo 只读）
.cargo-home/
