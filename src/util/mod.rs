//! Shared utilities.

// Phase 2 scaffolding: the kopia/ludusavi command wrappers are not wired up
// yet, so keep their dead-code warnings out of the build until then.
#[allow(dead_code)]
pub mod executor;
